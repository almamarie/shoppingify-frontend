import styles from ",/CurrentCart.module.css";
import { useSelector } from "react-redux";

const CurrentCart = () => {
  // fetch cart id from store
  const cartId = "01";

  return <h1>CurrentCart</h1>;
};

export default CurrentCart;
